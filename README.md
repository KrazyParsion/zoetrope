# Zoetrope
1. find_period finds the time it takes for one rotation
2. one_magnet_kirby and one_magnet_popcat are two zoetrope prototypes that uses popcat or kiby as the animation, both only using one magnet
